config:
  azure-native:location: westeurope
  pre-talx-tix-azure:prefix: pretalxtix
  pre-talx-tix-azure:pretixImageTag: stable
  pre-talx-tix-azure:pretalxImageTag: latest
  pre-talx-tix-azure:mailFrom: noreply@example.com
  # Custom domains — set these after first deploy and CNAME setup:
  # pre-talx-tix-azure:pretixCustomDomain: tickets.yourdomain.com
  # pre-talx-tix-azure:pretalxCustomDomain: talks.yourdomain.com
  # Explicit URLs (auto-derived from custom domains if not set):
  # pre-talx-tix-azure:pretixUrl: https://tickets.yourdomain.com
  # pre-talx-tix-azure:pretalxUrl: https://talks.yourdomain.com
